# Computer Use 控件评分：MPS 内存修复后的重跑

**已按用户要求暂停（01:35，UTC+8），等待之后改用 5090。** 本地训练进程已退出。最后记录为 40 次更新、320/2,068 条；最近完整保存的权重是 `step-0025/`（25 次更新、200 条），SHA-256 已核验。没有保存优化器状态，不是可精确恢复到中断瞬间的检查点。`pause.json` 记录暂停状态；此前 `failure.txt` 中的 KeyboardInterrupt 来自这次主动停止，不是新的内存故障。

2026-09-22 01:32（UTC+8）启动。当前阶段以 `status.json` 为准，结束后自动保存 `results.json`。

上一次 v1 在 21.3% 进度附近触及 28 GiB MPS 内存上限。由于没有训练检查点，此次从原始 Laya 权重重新开始，没有沿用那 440 条样本对应的参数更新。失败运行的日志完整保留在相邻 v1 目录。

本次仍训练全部 2,068 条控件评分对、1 轮，batch 2、梯度累积 4、259 次优化器更新。FP32、rank 16 LoRA 和决策头学习率不变。没有修改数据划分，没有读取校准集或试验留出集，也没有改动已有客服权重。

内存修复包括：

- 编码器与决策头均开启梯度重算，减少保存的中间激活。
- 输入在 CPU 上按 64-token 桶补齐，补齐位置的 attention mask 为 0，减少 MPS 为不同长度保留的形状缓存。真实输入和候选不截断。
- 每 25 次更新释放可释放的 MPS 缓存，并保存一次独立模型检查点。
- 开始更新前，对最长的两条真实输入（最长 1,417 tokens）连续进行两次完整前向/反向检查；补齐前后输出一致性也单独验证。结果保存在 `preflight.json`。
- MPS 内存限制仍为 28 GiB，没有禁用内存保护。

训练后在预先固定的 4 个开发页面、1,020 个完整候选上比较原始与新模型。只报告控件排名、Recall@1/8/24/64 和扫描耗时，不把小样本开发检查解释为网站任务成功率。概率尚未校准。

`source/` 归档了与 `run_config.json` 中 SHA-256 对应的训练代码；`initial/` 是此次原始参数，`step-*/` 为阶段性权重，`epoch-1/` 为完成整轮后的权重。模型检查点没有包含优化器精确续训状态。

查看当前进度：

```sh
cat results/computer-use-grounding-lora-v2/status.json
tail -f results/computer-use-grounding-lora-v2.log
```
