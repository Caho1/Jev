# 早期浏览器决策回放：8 步流程检查

2026-09-22。三个模型均完成同一组冻结输入的 8 次调用。
该协议是 Mind2Web `operation + item`，不是 jev-ultrafast 的按操作区分目标协议，
也不是模型自主执行网页后的成功率。本次没有更新参数或执行浏览器动作。

| 指标 | 原始 Laya | 上轮 computer use 微调 | Jev |
|---|---:|---:|---:|
| 操作匹配人工标注 | 5/8 | 5/8 | 5/8 |
| 目标题匹配，包括 NONE | 1/8 | 4/8 | 3/8 |
| 操作和目标同时匹配，包括 NONE | 0/8 | 1/8 | 2/8 |
| 目标确实存在且操作与目标均匹配 | 0/8 | 0/8 | 1/8 |
| 调用延迟中位数（靠上的中位样本） | 1,511 ms | 1,539 ms | 408 ms |

这里只有 4/8 步的正确目标进入候选，不能据此判断模型总体能力。
原始 Laya 与微调版在本机 MPS 逐题前向；Jev 一次 HTTP 处理同一步的两题。
延迟不含模型加载、浏览器观察、执行或等待，不能比较为端到端网页操作速度。
采样在预测前按网站和 ID 固定，不按答案和结果筛选。

[汇总](results.json)、[逐题完整输入及三个模型输出](cases.jsonl)、
[本地重算与隔离检查](local-audit.json)。训练教师试采另有 4 步，导出 8 个题目，
其中 6 个教师分布与人工答案一致；开发输出未混入训练。

后续按[jev-ultrafast 浏览器方案](../../research/browser-use-jev-ultrafast.md)推进。
本结果保留作历史诊断，不能改名为新协议评测结果。
