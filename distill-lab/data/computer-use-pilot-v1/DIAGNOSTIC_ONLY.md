# 仅用于检索诊断，不作为正式训练入口

前 24 个 BM25 候选在训练集的操作/目标覆盖率为 118/519。此处的 DEFER 标签仅表示人工示范目标未被召回，不能证明不存在其他合理动作；不得将其当作已验证的安全拒答标签直接训练。

已准备新的控件评分数据：`../computer-use-grounding-v1/`。新版本保留完整候选池，只在训练划分构造有监督正负样本。详见 `../../research/computer-use-finetuning-2026-09-22.md`。

本目录没有用于更新模型参数。保留它是为了审计检索失败和避免之后误报实验结果。
