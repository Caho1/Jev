# Jev macOS 本机测试与修复

日期：2026-09-19。仓库：https://github.com/Caho1/jev-computer-use ，基线提交 `ca8a425`。
安装目录：`/Users/bystanders/.agents/skills/jev-mac-use`。

## 结论

Jev 在所测人工 AX 场景中能够以约 0.3 秒的中位数延迟选择正确操作。原生 Swift 执行器与 Demo 在本机成功编译，Demo 成功启动。
用户手动授权后，辅助功能权限为 `true`。真实 Demo 连续 10/10 轮通过，每轮包含输入、点击和最终 AX 核验，中位数约 0.857 秒。另通过 Codex 原生 UI 读取确认最终查询框为 `penguin`，结果为 `Results for: penguin`。这仅代表该简单 Demo，不代表任意应用成功率，也没有与 Codex 内置 Computer Use 做速度对照。

## 实测数据

本机 Darwin arm64，Apple Swift 6.4，Python 3.9.6。仓库文档要求 Python 3.10+，本次已有测试与 API benchmark 在系统 Python 3.9.6 上通过，不代表全面兼容性承诺。

模型：`jev-1.13.0`。一次冷启动加 60 次稳定请求，10 种独立人工场景，每模式每场景重复 3 次，顺序随机打散。耗时包括网络。

| 模式 | 决策样本数 | p50 | p95 | 最大值 | 符合预期 / 通过阈值 |
| --- | ---: | ---: | ---: | ---: | ---: |
| flat 完整动作选择 | 30 | 299.866 ms | 472.161 ms | 697.425 ms | 30/30 |
| fanout 操作与目标并行判断 | 30 | 305.515 ms | 431.700 ms | 487.489 ms | 30/30 |

冷启动：1111.853 ms。HTTP/传输失败：0/61。60 次测试决策全部达到原有概率和 margin 门槛，未降低阈值。

两种模式中位数接近；fanout 本轮尾延迟略低，但样本量不足以证明稳定优势，暂保留 flat 默认值。人工场景正确率不能推广为真实桌面成功率；这也不是与其他 Computer Use 工具的对照实验。

原始数据：`benchmark-macos.json`，包含每次请求的计时、概率和 token 统计，不含 API key。原脚本把环境硬编码成 cloud runner；该报告仅更正环境描述，保留全部测量值。

## 已修复

1. **缺少辅助功能权限仍先调用收费 API**：现在在 API client 创建和预热前读取一次本地快照，先检查权限、应用与窗口；预热后重新观察，不复用过期句柄。
2. **截断文本误判为无需修改**：只有完整观测值与目标文本相同时才跳过写入，避免保留未观测到的多余后缀。
3. **低置信度停止丢失计时**：现在保留 `decision_rejected`、API 时延、循环时延与失败原因，便于统计回退，而不保存 UI 文本。
4. **fanout 目标概率被元素 ID 覆盖**：使用独立 `target_metrics` 字段保留目标置信度。
5. **测速环境误标为云端**：改为记录实际系统、架构与 Python 版本。

改动留在已安装仓库的工作区，未提交或推送。可迁移补丁：`jev-mac-use-fixes.patch`。

## 验证

- 原有 17 项单元测试通过。
- 新增 4 项回归测试，覆盖权限失败前不创建 API client、截断值写入、拒绝决策记录计时、fanout 目标概率保留。总计 21 项通过。
- `git diff --check` 通过。
- 修复后真实 CLI 在无 key、无辅助功能权限时优先报告 `AX: accessibility_permission_required`，不再先进入 API 配置/预热。
- Swift 与 Demo 构建通过；本轮没有修改 Swift 源码。

## 已完成的桌面验收

权限由用户手动开启，doctor 返回 `accessibility_trusted: true`。执行器未重新编译，使用本轮原已编译的同一版本。

测试脚本 `native-benchmark.py` 用同一个 Jev HTTPS 连接与 AX 进程，随机交替测试两种模式各 5 轮。每轮通过观察到的 Reset 控件复位，并确认查询框为空且状态为 Ready，再进行两步操作。没有复用旧的成功状态。失败立即停止，不自动重放失败动作。

| 模式 | 通过 / 总数 | 完整任务 p50 | 样本 p95 |
| --- | ---: | ---: | ---: |
| flat | 5/5 | 857.296 ms | 910.506 ms |
| fanout | 5/5 | 857.344 ms | 933.775 ms |

耗时包括观察、Jev 决策、AX 执行、等待界面稳定及最终核验；不含编译、启动进程、复位或首次预热。首次连接预热另计 1106.241 ms；第一轮任务 857.296 ms，两者相加约 1.964 秒，仍不含启动等开销。每组只有 5 轮，所报 p95 实际为该组最大值，不宜推断长期尾延迟。

20 个任务动作均为预期的先 set_value 再 press，无错误动作、无回退、无超时。最后通过独立的 Codex 原生 UI 读取再次确认查询框 `penguin` 和 `Results for: penguin`。此处 10/10 是同一个简单 Demo 的重复结果，并非多应用泛化成功率。

详细逐步计时、置信度与 token 数据在 `native-benchmark-macos.json`。API key 仅通过隐藏输入进入进程内存，没有写入脚本或结果文件。

两模式在真实 Demo 中位数几乎相同，维持 flat 默认。主要耗时来自每步网络/API 决策，其次为原生按钮操作和界面稳定等待；当前不应为追求数字而删掉最终核验或盲目缩短稳定等待。
